/**
 * Chart-side uncertainty primitives — barrel export.
 *
 * The *statistics* live in `@/analysis/uncertainty`; this folder holds the
 * *rendering* adapters that shape them for charts (consensus D3).
 *
 * @module components/charts/uncertainty
 */

export * from './rollingBandSeries';
